management:
  endpoints:
    web:
      exposure:
        include: "*"
        
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
      uri: http://localhost:8888
      
my:
  message: Hello world client
